export interface Student {
    id: number;
    first_name: string;
    last_name: string;
    date_of_birth: string; // You might want to use a Date type if you'll be working with dates
    email: string;
    address: string;
    phone_number: string;
}
